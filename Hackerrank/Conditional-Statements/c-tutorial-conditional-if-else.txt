#include <bits/stdc++.h>

using namespace std;

string ltrim(const string &);
string rtrim(const string &);



int main()
{
    string n_temp;
    getline(cin, n_temp);

    int n = stoi(ltrim(rtrim(n_temp)));
        switch(n)
        {
            case(1):
            {
                printf("one");
                break;
            }
            case(2):
            {
                printf("two");
                break;
            }
           case(3):
            {
                printf("three");
                break;
            }
            case(4):
            {
                printf("four");
                break;
            }
            case(5):